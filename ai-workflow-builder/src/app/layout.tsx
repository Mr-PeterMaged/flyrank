import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AI Workflow Builder | Peter Maged",
  description: "Visual AI decision workflows, designed and developed by Peter Maged.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}<footer className="pm-credit"><div>Developed by <a href="https://petermaged.com/">peter maged</a></div><div>© 2026 PeterMaged. All rights reserved.</div></footer></body>
    </html>
  );
}
